apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod-environment-root
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/asmabadrwork/gitops-manifests.git'
    targetRevision: HEAD
    path: argocd
    directory:
      recurse: true
      exclude: 'parents/*'
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
